name: Release

on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact: den.exe
            ext: zip
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            artifact: den
            ext: tar.gz
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6

      - name: Install Rust toolchain
        run: rustup target add ${{ matrix.target }}

      - name: Build release binary
        run: cargo build --release --target ${{ matrix.target }}

      - name: Package (Windows)
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          $bin = "target/${{ matrix.target }}/release/${{ matrix.artifact }}"
          Compress-Archive -Path $bin -DestinationPath "den-${{ matrix.target }}.zip"

      - name: Package (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: |
          cd target/${{ matrix.target }}/release
          tar czf "../../../den-${{ matrix.target }}.tar.gz" ${{ matrix.artifact }}

      - name: Upload to release
        run: gh release upload ${{ github.ref_name }} den-${{ matrix.target }}.${{ matrix.ext }} --clobber
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
